apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: default
  labels:
    app: reservation-service
spec:
  type: LoadBalancer
  selector:
    app: reservation-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084