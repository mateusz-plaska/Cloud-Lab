apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: default
  labels:
    app: shipping-service
spec:
  type: ClusterIP
  selector:
    app: shipping-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085